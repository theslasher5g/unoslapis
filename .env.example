# Kopieren nach .env und bei Bedarf anpassen
DOMAIN=unoslapis.ch

# Geheimes Passwort zum Löschen von Gästebuch-Einträgen (beliebiger langer Text)
# Erzeugen z.B. mit: openssl rand -hex 24
GUESTBOOK_ADMIN_TOKEN=
